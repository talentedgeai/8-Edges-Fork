// The home page's Core Solutions section.
//
// This file is an overlay stub for 8-Edges-Fork. It only neutralises upstream
// while it sits at the SAME repo-relative path as the real component — today
// entities/site/ui/CoreSolutions.tsx.
//
// Fork note: three rows selling the upstream's service pages, each ending in a
// Learn more link to one. The pages are internal, so the home page renders this
// only when HAS_SERVICE_PAGES — false here. Stubbed rather than left in place
// because the copy itself is the upstream's: its rates, its talent model and
// its retreat, which a fork has no reason to carry even as dead source.
export function CoreSolutions() {
  return null;
}
