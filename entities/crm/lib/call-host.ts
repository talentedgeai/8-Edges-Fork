// Fork overlay stub: no host is named, so the call scorecard reads every call
// as host-absent until the client names its own sellers here. Replaces
// entities/crm/lib/call-host.ts in the sync.
export const HOST_RE = /^$/;
