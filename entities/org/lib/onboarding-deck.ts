// Fork overlay stub: the private workflows library does not ship, so there is
// no onboarding deck to embed. Replaces entities/org/lib/onboarding-deck.ts.
export const ONBOARDING_DECK_PATH: string | null = null;
