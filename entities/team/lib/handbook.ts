// Fork overlay stub: a client deployment has no employee handbook link to put
// on the new hire's home, so the tile is not shown. Replaces
// entities/team/lib/handbook.ts in the sync.
export const HANDBOOK_URL: string | null = null;
