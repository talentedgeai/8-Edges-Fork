// Route file: the body lives in entities/company-os/routes/(dashboard)/talent/applications/new/page.tsx (ME-12).
// Segment config stays here: Next reads it from the route file's own
// `export const` declarations and never through a re-export.
export { default, metadata } from "@/entities/company-os/routes/(dashboard)/talent/applications/new/page";

export const dynamic = "force-dynamic";
export const fetchCache = "force-no-store";
