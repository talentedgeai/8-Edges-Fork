// Thin composer: the page lives in the billing entity (ME-05). `metadata` is
// re-exported by name because Next.js reads it from the route file itself.
export { default, metadata } from "@/entities/billing/routes/plans/retreats-pnl.html/page";
